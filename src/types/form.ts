export interface IForm {
  email: string;
  password: string;
}

export interface IAuthForm {
  title: string;
  buttonText: string;
  subtitleText: string;
  linkText: string;
}

export interface IProjectForm {
  name: string;
  description: string;
  domain: string;
  start_date: string;
  team_size: string;
}
